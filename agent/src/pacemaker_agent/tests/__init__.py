"""Test-Harness fuer den Agenten (Synthetic Caller, Fixture-Generator).

Nicht zu verwechseln mit dem pytest-Verzeichnis `agent/tests/` (Unit-Tests).
Dieses Package liegt unter `src/pacemaker_agent/tests/`, damit
`python -m pacemaker_agent.tests.synthetic_caller` als installiertes Modul
funktioniert (siehe CLAUDE.md).
"""
